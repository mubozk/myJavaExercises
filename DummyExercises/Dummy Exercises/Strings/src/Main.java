public class Main {
    public static void main(String[] args) {
        String message = "Hello World" + "!!"; //Strings are reference types but this is just a shortcut by not calling a class
        System.out.println(message.endsWith("!!"));// endsWith&startsWith are boolean values that checks the starting and finishing lines of strings
        // lenght that gets string is the same
        // indexOf gets the indexOf the desired character's position
        // replace ((""), (""))
        // toLowerCase to lower Upper chars
        // trim() removes the spaces on start
        // use \ to escape even other \ characters
    }
}